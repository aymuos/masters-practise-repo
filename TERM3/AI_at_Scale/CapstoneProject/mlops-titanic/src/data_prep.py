# Loads Titanic data, handles missing values, encodes categorical features, splits data, 
# and writes processed outputs (ideally via Spark). Accepts CLI flags and reads params.yaml for schema/feature toggles.
